<?php defined( 'ABSPATH' ) || die( '-1' ); // don't load directly ?>

<div
	class="notice slate-notice notice-info is-dismissible"
	data-dismiss-url="<?php echo esc_url( $dismiss_url ); ?>">

	<h2><?php echo 'SVG Logo and Text Effects Raster Fallbacks extension'; ?></h2>

	<p><?php echo 'SVG Logo and Text Effects'; ?> can be extended with the
	<a target="_blank" href="https://www.dashed-slug.net/svg-logo-and-text-effects-extensions/raster-fallbacks-extension/">SVG Logo And Text Effects Raster Fallbacks extension</a> to provide <code>.PNG</code> raster fallbacks to legacy browsers that cannot render SVG.</p>

</div>
